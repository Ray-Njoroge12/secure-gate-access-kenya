import { describe, it } from 'vitest';

// Deprecated: Supabase edge function tests removed in React-only stub mode.
// This placeholder intentionally skipped to avoid legacy Supabase coupling.

describe.skip('create-invitation edge function (removed)', () => {
  it('placeholder', () => {});
});
