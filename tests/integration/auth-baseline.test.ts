import { describe, it, expect } from 'vitest';

// Entire legacy auth-baseline test removed (Supabase dependency). Disabled after Supabase removal.
describe.skip('Phase 1 - Core Data & Auth Baseline (DISABLED - Supabase removed)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});

export {};
