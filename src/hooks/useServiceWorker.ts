import { useEffect } from 'react';

// Placeholder hook. You can extend this to handle service worker updates/refresh prompts.
export function useServiceWorker() {
  useEffect(() => {
    // No-op for now
  }, []);
}
